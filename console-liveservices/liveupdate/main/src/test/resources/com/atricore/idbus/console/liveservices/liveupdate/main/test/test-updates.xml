<?xml version="1.0" encoding="UTF-8"?>

<updatesIndex xmlns="urn:com:atricore:liveservices:liveupdate:1.0:md"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:com:atricore:liveservices:liveupdate:1.0:md
                    http://www.atricore.com/liveservices/liveupdate/atricore-liveupdate-md-1.0.xsd"
        ID="id0000001200">

    <updateDescriptor ID="id0000000200" description="JOSSO 2 EE ${pom.version}" IssueInstant="2010-12-25T19:32:52Z">

        <installableUnit ID="id0000000120" group="${pom.groupId}" name="${pom.artifactId}" updateNature="UPGRADE">

            <version Version="${pom.version}"/>

            <!-- Provided Features -->
            <feature ID="id000000232" group="${pom.groupId}" name="josso-ee" description="JOSSO 2 EE ${pom.version}">
                <version Version="${pom.version}"/>
            </feature>

            <!-- Set of artifacts required for this install -->

            <!-- JOSSO 2 EE Update (distribution) -->
            <artifact ID="id0000000121" group="${pom.groupId}" name="josso-ee" version="${pom.version}" classifier="liveupdate"/>

            <!-- JOSSO 2 EE Update (liveupdate installer) -->
            <artifact ID="id0000000122" group="${pom.groupId}" name="josso-ee" version="${pom.version}" classifier="install"/>


            <!-- Required features -->
            <requirement group="${pom.groupId}" name="josso-ee" description="JOSSO 2 EE 2.0.0" >
                <versionRange Expression="[1.9,2.0)"/>
            </requirement>

        </installableUnit>

    </updateDescriptor>

</updatesIndex>