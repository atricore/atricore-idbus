akka {
  loglevel = DEBUG
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
}

# check the reference.conf in the spray-server project main/resources for all defined settings
spray.servlet.boot = ["org.atricore.idbus.proxy.Boot"]