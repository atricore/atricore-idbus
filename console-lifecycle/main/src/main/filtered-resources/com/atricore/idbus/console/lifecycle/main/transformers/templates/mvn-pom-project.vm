<?xml version="1.0" encoding="UTF-8"?>

<!-- Generated by Atricore Identity Bus Server ${atricore.idbus.version} b${buildNumber} -->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>$module.getGroup()</groupId>
    <artifactId>$module.getName()</artifactId>
    <version>$module.getVersion()</version>
    <packaging>pom</packaging>

    <name>$applianceDef.getDescription()</name>

    <properties>

        <atricore.idbus.version>${atricore.idbus.version}</atricore.idbus.version>
        <atricore.idbus.revision>${buildNumber}</atricore.idbus.revision>

        <!-- Atricore Kernel -->
        <idbus.kernel.version>${idbus.kernel.version}</idbus.kernel.version>

        <!-- Atricore Capabilities -->
        <idbus.cap.josso.version>${idbus.cap.josso.version}</idbus.cap.josso.version>
        <idbus.cap.saml2.version>${idbus.cap.josso.version}</idbus.cap.saml2.version>
        <idbus.cap.sts.version>${idbus.cap.sts.version}</idbus.cap.sts.version>
        <idbus.cap.spml2.version>${idbus.cap.spml2.version}</idbus.cap.spml2.version>
        <idbus.cap.openid.version>${idbus.cap.openid.version}</idbus.cap.openid.version>

        <!-- Atricore IDOJOs -->
        <idbus.idojo.dbidentitystore.version>${idbus.idojo.dbidentitystore.version}</idbus.idojo.dbidentitystore.version>
        <idbus.idojo.ehcachesessionstore.version>${idbus.idojo.ehcachesessionstore.version}</idbus.idojo.ehcachesessionstore.version>
        <idbus.idojo.ldapidentitystore.version>${idbus.idojo.ldapidentitystore.version}</idbus.idojo.ldapidentitystore.version>
        <idbus.idojo.memoryidentitystore.version>${idbus.idojo.memoryidentitystore.version}</idbus.idojo.memoryidentitystore.version>
        <idbus.idojo.memorysessionstore.version>${idbus.idojo.memorysessionstore.version}</idbus.idojo.memorysessionstore.version>
        <idbus.idojo.serializedsessionstore.version>${idbus.idojo.serializedsessionstore.version}</idbus.idojo.serializedsessionstore.version>
        <idbus.idojo.strongauthscheme.version>${idbus.idojo.strongauthscheme.version}</idbus.idojo.strongauthscheme.version>

        <idbus.connector.jdoidentityvault.version>${idbus.connector.jdoidentityvault.version}</idbus.connector.jdoidentityvault.version>

        <!-- Atricore Bundles -->
        <idbus.bundle.josso-ws-v1_2.version>${idbus.bundle.josso-ws-v1_2.version}</idbus.bundle.josso-ws-v1_2.version>

        <felix.plugin.version>2.1.0</felix.plugin.version>

        <!-- 3rd party components -->
        <apache-wicket.version>${apache.wicket.version}</apache-wicket.version>

    </properties>

    <distributionManagement>

        <repository>
            <id>deploy-repo</id>
            <name>Identity Appliance Deployment Repository</name>
            <url>file://$karafHome/appliances/</url>
        </repository>

    </distributionManagement>

    <build>

        <pluginManagement>
            <plugins>
                
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>2.3</version>
                    <configuration>
                        <source>1.5</source>
                        <target>1.5</target>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>2.3</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>2.3</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>2.4</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>2.3</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>2.5</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>2.4.2</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.felix</groupId>
                    <artifactId>maven-bundle-plugin</artifactId>
                    <version>${felix.plugin.version}</version>
                    <extensions>true</extensions>
                </plugin>

            </plugins>

        </pluginManagement>
    </build>

    <modules>
#foreach ($module in $module.getModules())
        <module>$module.getId()</module>
#end
    </modules>
</project>
