<?xml version="1.0" encoding="UTF-8"?>

<!-- Generated by Atricore Identity Bus Server ${atricore.idbus.version} b${buildNumber} -->

<features name="$applianceDef.getName()-$module.getVersion()">
#foreach ($module in $module.getParent().getModules())
#if ($module.getType().equals("Appliance"))
    <!-- $module.getId() : $module.getDescription() Identity Appliance Unit -->
    <feature name="$applianceDef.getName()-$module.getId()-idau" version="1.0.$applianceDef.getRevision()">
        <feature version="${atricore.idbus.version}">atricore</feature>
#foreach ($module1 in $module.getParent().getModules())
#if ($module1.getType().equals("Federation"))
        <bundle>mvn:$module1.getGroup()/$module1.getName()/$module1.getVersion()</bundle>
#end
#end
    </feature>
#end
#end
</features>
